#pragma once
#include <string>
#include <set>
#include "item_location.hpp"

#include "../code/include/z3D/z3D.h"

namespace Playthrough {
    int Fill(u32 seed);
    s16 GetRandomPrice();
}
